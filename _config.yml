# Site Build Settings

# Jekyll-paginate-v2 configuration
#  pagination:
#   enabled: true
#   per_page: 5
#   permalink: '/page/:num/'
#   title: ':title - page :num of :max'
#   limit: 0
#   sort_field: 'date'
#   sort_reverse: true

pagination:
  enabled: true
  per_page: 5  # Adjust the number of posts per page as needed
  sort_field: 'title'

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           /:title/
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           'Journey'
description:	       'a technical blog'
author:              'Krithik Vaidya'

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
